<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <version>2</version>

  <readers>
    <reader>
      <classname>AcquisitionReader</classname>
      <dll>gadgetron_core_readers</dll>
    </reader>
  </readers>

  <stream>

    <!-- This 'external' node sends data to the visualizer. -->
    <external>
      <connect port="18000"/>
      <configuration/>
    </external>

  </stream>

</configuration>
